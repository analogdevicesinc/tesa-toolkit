# Python packages that require to use python scripts in this repository

# used to generate ECDSA signature for MAX32657 MCUBoot that validated by BootROM
ecdsa>=0.19.0

# used by Secure Boot ROM provisioning fw
pyelftools>=0.31
